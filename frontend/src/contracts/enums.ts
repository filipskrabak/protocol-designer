export enum Endian {
  Little = "little",
  Big = "big",
}

export enum EditingMode {
  Add = "add",
  Edit = "edit",
}

export enum AddFieldPosition {
  Before = "before",
  After = "after",
  End = "end",
}
